; По умолчанию php-fpm отбрасывает stdout/stderr воркеров — Yii::error()
; (пишет в php://stderr, см. backend/config/common.php) никогда не долетал
; бы до `docker compose logs backend`, только собственный access-лог fpm.
[www]
catch_workers_output = yes
decorate_workers_output = no
