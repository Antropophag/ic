# HTTP API

Базовый префикс: `/api/v1`. Формат запросов и ответов — JSON. Ошибки валидации
возвращают `422`, отсутствие аутентификации — `401`, ошибки workflow — `409`.

Для любой загрузки файла (`.../documents`, `.../report`) `client_max_body_size`
шлюза (`docker/nginx/default.conf`) выставлен на 11 МБ — с запасом над
10-мегабайтным лимитом приложения (COM-007/DOC-002A). Запрос крупнее этого
получает `413` от nginx раньше проверки приложения; такой ответ не в формате
JSON API, фронтенд обрабатывает его отдельно от `422`.

## Аутентификация

Вне `APP_ENV=dev` запросы используют серверную сессию, созданную после LDAP
bind (AUTH-001..005, `docs/business-rules.md`, `docs/architecture.md#ldap`).
При `APP_ENV=dev` заголовок `X-Dev-User-ID` по-прежнему принимается и заменяет
собой сессию; в остальных окружениях он игнорируется.

### `GET /api/v1/auth/me`

Не требует входа. Всегда возвращает CSRF-токен (устанавливает CSRF-cookie в
ответе — фронтенд обязан получить токен до первой попытки логина) и признак
`devMode`, отражающий `APP_ENV` бэкенда:

```json
{"csrfToken": "...", "devMode": false, "user": null}
```

`user` — профиль текущей сессии (`null`, если сессии нет или локальный профиль
отключён). Используется фронтендом при загрузке приложения, чтобы решить,
показывать dev-переключатель профиля или форму входа.

### `POST /api/v1/auth/login`

```json
{"login": "ivanov", "password": "..."}
```

Выполняет LDAP bind и возвращает профиль вошедшего пользователя вместе с
новым CSRF-токеном (сессия, соответственно и токен, меняются при входе).
`401` с `ruleId: "AUTH-001"` — неверный логин или пароль; `403` с
`ruleId: "AUTH-003"` — учётная запись отключена локально; `422` — пустой
логин или пароль; `500` — LDAP-сервер недоступен. Требует CSRF-токен вне dev.

### `POST /api/v1/auth/logout`

Завершает сессию и возвращает новый CSRF-токен:

```json
{"csrfToken": "...", "user": null}
```

### Dev-режим

При `APP_ENV=dev` фронтенд выбирает актёра переключателем профиля в шапке
и подставляет его в заголовок `X-Dev-User-ID` на каждый запрос. Список
профилей для переключателя запрашивается через `GET /api/v1/auth/dev-users`
(доступен только при `APP_ENV=dev`, вне dev отвечает 404), а не хардкодится
во фронтенде — фиксированный id не гарантирован (см. ниже). Выбор сохраняется
в `localStorage` браузера. Смена профиля закрывает открытую карточку заявки
и перезагружает реестр, чтобы капабилити и вкладка «Мои заявки» пересчитались
под нового актёра.

`dev/seed` создаёт шесть основных профилей (руководитель ИЦ, исполнитель,
сотрудник, эксперт, сотрудник СБ, администратор), на смок-тестах и в CI —
всегда под фиксированными id 1–6, на которые ссылаются smoke-тесты и E2E. На
персистентной демо-базе фиксированный id мог уже органически занять другой
пользователь (например, органически выросший `AUTO_INCREMENT` или один из
дополнительных исполнителей ниже) — в этом случае `dev/seed` не прерывается,
а сидирует конфликтующий основной профиль по его уникальному `ad_login`
вместо фиксированного id и печатает предупреждение в консоль. Именно поэтому
фронтенд резолвит актуальные id через `GET /api/v1/auth/dev-users`, а не
предполагает id 1–6. Второй эксперт (нужен только для проверки
переназначения заявки между экспертами) заведён без фиксированного id и не
входит в список переключателя, а резолвится отдельно на экране назначения
через `GET /api/v1/experts`. Ещё шесть исполнителей ИЦ по списку ТЗ 7.5
(Наумов, Прикуль, Шапошников, Галкин, Козлов, Нелидова) заведены так же, без
фиксированного id, по тем же причинам.

## Заявки

### `GET /api/v1/executors`

Возвращает только активных пользователей с ролью `Исполнитель ИЦ`. Список
используется серверно управляемой формой назначения.

### `GET /api/v1/requests`

Возвращает последние доступные пользователю заявки:

```json
{
  "items": [
    {
      "id": 1,
      "number": 1,
      "status": "registered",
      "lockVersion": 1,
      "product_name": "Образец продукции",
      "initiator_name": "Иван Иванов"
    }
  ]
}
```

Каждый элемент также содержит текущее назначение, `lock_version`, `color`
(ручная цветовая метка руководителя), `security_mark` (`approve`/`return`/
`null` — решение последнего контроля СБ, если он проводился) и вычисленные
сервером признаки доступных действий, включая `can_security_decide`. Frontend
использует их только для отображения элементов управления; API повторно
проверяет полномочия.

### `GET /api/v1/requests/{id}`

Возвращает карточку заявки и хронологию разрешённых успешных событий. Карточка
доступна любому активному авторизованному сотруднику. Для отсутствующей заявки,
несуществующего или отключённого локального профиля возвращается одинаковый
ответ `404`, не раскрывающий наличие записи.

В `history` входят процессные переходы и успешное назначение исполнителя с
идентификатором правила, автором и временем. Технический `payload_json`,
отклонённые действия и секретные данные аудита через этот endpoint не выдаются.
Для возврата из СБ переход также содержит пользовательскую причину в `reason`.
Для `assign_executor`/`claim_expert`/`reassign_expert` дополнительно
возвращается `targetName` — имя сотрудника, которого назначили (для
`claim_expert` совпадает с автором действия, отдельно не используется);
резолвится через `request_assignments` по `assignment_id` из аудита, а не
через сырой `payload_json`. Для остальных действий (включая `delete_report`)
`targetName` — `null`.
Для `upload_report` история также содержит `versionId` и `originalName`, если
пользователю разрешено видеть соответствующую версию отчёта. Для остальных
событий и при отсутствии доступа эти поля равны `null`; скачивание по
`versionId` повторно проверяет те же серверные права доступа.
Поле `comments` содержит комментарии в хронологическом порядке с назначенными
сервером автором и временем. Признак `item.can_comment` сообщает интерфейсу,
доступна ли форма комментария в текущем статусе; сервер в любом случае повторно
проверяет правило при записи. Карточка возвращает не более 50 последних
комментариев. `commentsPage.hasMore` и `commentsPage.nextBeforeId` указывают,
доступны ли более ранние записи.
Поле `documents` содержит только последние версии документов и не раскрывает
физические пути или ключи хранилища. `item.can_upload_document` управляет
видимостью загрузки, но не заменяет серверную проверку.
Как и в `GET /api/v1/requests`, `item` содержит `color` и `security_mark`
(`approve`/`return`/`null` — решение последнего контроля СБ).

### `GET /api/v1/requests/{id}/comments?beforeId={id}`

Возвращает до 50 более ранних комментариев перед указанным идентификатором.
Ответ содержит `items`, `hasMore` и следующий `nextBeforeId`; это ограничивает
размер карточки и DOM независимо от общей длины обсуждения.

### `POST /api/v1/requests/{id}/comments`

Добавляет неизменяемый текстовый комментарий:

```json
{"body": "Образцы переданы в испытательный центр."}
```

Пустой текст и текст длиннее 10 000 символов возвращают `422`. Комментарии
разрешены от регистрации до решения СБ включительно; после закрывающего решения
API возвращает `409`. Автор и время берутся только из серверного контекста.
Текст сохраняется в таблице комментариев, но не копируется в технический аудит.

### `POST /api/v1/requests/{id}/documents`

Принимает `multipart/form-data` с полем `file`. Разрешены PDF, PNG, JPEG, DOCX и
XLSX размером от 1 байта до 10 МБ; расширение проверяется вместе с MIME,
определённым по содержимому. Для DOCX и XLSX дополнительно проверяется структура
пакета Office и ограничивается его распакованный размер. Обычный ZIP с подменённым
расширением отклоняется. Повторная загрузка с тем же именем создаёт новую
неизменяемую версию. Успешный ответ `201` содержит метаданные и SHA-256, но не
содержит физический ключ. `409` означает закрытый этап, `422` — недопустимый файл.

### `GET /api/v1/document-versions/{id}/download`

Возвращает байты конкретной версии только активному авторизованному сотруднику.
До завершения заявки отчёт испытаний доступен только назначенному исполнителю и
руководителям ИЦ/лаборатории. Заключение доступно назначенному эксперту, автору
конкретной редакции, руководителям и сотрудникам СБ; посторонний сотрудник
получает тот же `404`, что и для отсутствующей версии.
Файл выдаётся как attachment с исходным безопасным именем; успешное скачивание
попадает в аудит. Отсутствующая версия и отсутствующий физический файл дают
одинаковый `404`.

### `GET /api/v1/document-links/{token}/download`

Активная ссылка из письма-уведомления (ТЗ 4.6/4.9/4.10): не требует входа в
портал и работает, даже пока обычный доступ к документу ещё не открыт (ACL-003
..006). Токен из 64 hex-символов выдаётся сервером при постановке письма в
очередь и относится к конкретной версии документа; в БД хранится только его
хэш. Неизвестный токен или отсутствующий физический файл дают `404`.

### `POST /api/v1/requests/{id}/report`

Принимает PDF-отчёт в поле `file` размером до 10 МБ. Загрузка доступна назначенному
исполнителю и руководителям ИЦ/лаборатории для заявки в работе или на подготовке
заключения, а также для выполненной заявки (`completed`), если действующий отчёт
был удалён (DOC-011/DOC-012, ТЗ 7.8). Первая загрузка (или повторная после удаления)
всегда атомарно увеличивает `lockVersion` и переводит заявку в `opinion_preparation`.
Это относится и к импортированной заявке, уже находящейся в `opinion_preparation`.
Обычная повторная загрузка (без предшествующего удаления) создаёт неизменяемую
версию того же отчёта без смены статуса.
Ответ `201` возвращает безопасные метаданные, новый статус и версию блокировки;
`403` означает нарушение DOC-001, `422` — неподходящий файл по DOC-002A.

### `POST /api/v1/requests/{id}/report/delete`

Удаляет действующий отчёт заявки (DOC-011, ТЗ 7.8). Тело: `{ "lockVersion": <int> }`.
Доступно назначенному исполнителю и руководителям ИЦ/лаборатории независимо от
статуса заявки, включая уже выполненную. Удаление не создаёт запись в истории
переходов статуса (статус не меняется), но записывается в аудит и в ленту истории
карточки. Удалённый отчёт и все его ревизии перестают быть доступны для скачивания
как через портал, так и по ранее выданным email-ссылкам; повторная загрузка отчёта
переиспользует тот же документ и запускает цикл эксперт → СБ заново (DOC-012).
Ответ `200` возвращает статус и новую версию блокировки; `403` означает нарушение
DOC-011 (нет прав или нет действующего отчёта для удаления), `409` — конфликт версии.

### `POST /api/v1/requests`

```json
{
  "productName": "Образец продукции",
  "manufacturer": "Производитель",
  "supplier": "Поставщик",
  "sampleQuantity": 1,
  "testMethod": "Типовая программа испытаний"
}
```

Номер назначается транзакционно. Инициатор и время берутся только из серверного
контекста. Успешный ответ имеет статус `201` и заголовок `Location`.
Сотрудник испытательного центра (`Исполнитель ИЦ`, `Руководитель ИЦ`,
`Руководитель лаборатории`) не может подать заявку; попытка возвращает `403`
с правилом `REQ-001` (business-rules.md).

### `POST /api/v1/requests/{id}/executor`

Назначает активного пользователя с ролью `Исполнитель ИЦ`:

```json
{
  "executorId": 42,
  "lockVersion": 1
}
```

Действие доступно руководителю ИЦ и руководителю лаборатории. Успешное и
отклонённое назначения записываются в аудит. Повторное назначение закрывает
предыдущую запись истории и создаёт новую в одной транзакции. Команда принимает
версию из реестра, увеличивает `lockVersion` и работает, пока заявка в статусе
`Заявка зарегистрирована`, `В работе` или `Работы приостановлены` (WF-012) —
то есть переназначить исполнителя можно и после того, как он уже начал работу,
без смены статуса заявки. Письмо новому исполнителю отражает фактический
статус (для уже начатой или приостановленной работы не предлагает «принять
её в работу»). Назначение уже закреплённого исполнителя отклоняется как
no-op (`403`, WF-013) — оно не меняет состояние, но без проверки всё равно
создавало бы лишнюю запись истории и письмо.

Ответ `403` содержит идентификатор нарушенного правила `WF-001`, `WF-002` или
`WF-013`, `409` означает конкурентное изменение или неподходящий статус.

Ответ `403` содержит идентификатор нарушенного правила `WF-001` или `WF-002`,
`404` означает отсутствие заявки или пользователя, `409` — конкурентное
изменение или смену статуса, `422` — ошибку тела запроса.

### `GET /api/v1/experts`

Возвращает активных пользователей с ролью `Эксперт` для серверно управляемого
списка переназначения.

### `POST /api/v1/requests/{id}/expert/claim`

Эксперт сам берёт заявку в работу на этапе `opinion_preparation` — никто её
не назначает (ТЗ 4.6/4.7):

```json
{
  "lockVersion": 4
}
```

Доступно любому активному пользователю с ролью `Эксперт`, если заявка сейчас
не закреплена за ним самим (иначе действие не имеет смысла). Закрывает
прежнее назначение, если оно было, и создаёт новое в одной транзакции.
Успешный ответ содержит новую `lockVersion`; `403` соответствует правилам
`WF-010` или `DOC-005`, `409` — конкурентное изменение, `404` — отсутствие
заявки.

### `POST /api/v1/requests/{id}/expert/reassign`

Текущий эксперт явно передаёт заявку конкретному другому активному эксперту
(цель не может совпадать с заявителем). Перехват заявки, закреплённой за
третьим экспертом (например, тот в отпуске), выполняется не этим
эндпоинтом, а повторным вызовом `claim` — self-assign работает независимо
от того, кто сейчас держит заявку:

```json
{
  "expertId": 42,
  "lockVersion": 4
}
```

Доступно только эксперту, за которым заявка закреплена сейчас. Сервер
блокирует заявку, проверяет актуальную версию, что заявитель — текущий
эксперт, и что цель — активный пользователь с ролью `Эксперт`. Целевой
эксперт получает письмо со ссылкой на отчёт. Успешный ответ содержит новую
`lockVersion`; `403` соответствует правилам `WF-011` или `DOC-005`, `409` —
конкурентное изменение, `404` — отсутствие заявки или эксперта.

### `POST /api/v1/requests/{id}/security-decision`

Сохраняет решение активного сотрудника СБ по актуальной редакции заключения:

```json
{"decision": "return", "reason": "Уточнить вывод по испытаниям.", "lockVersion": 6}
```

`approve` завершает заявку, `return` возвращает её в работу и требует причину до
5 000 символов. Решение, переход, аудит и увеличение `lockVersion` выполняются в
одной транзакции; повторное или устаревшее действие отклоняется. `403` означает
неверную роль или этап, `409` — конкурентное изменение, `422` — ошибку тела.

### `POST /api/v1/requests/{id}/start`

Переводит зарегистрированную заявку в работу:

```json
{
  "lockVersion": 1
}
```

Действие доступно активному текущему назначенному исполнителю, активному
руководителю ИЦ и активному руководителю лаборатории. Текущий `lockVersion`
клиент получает в `GET /api/v1/requests`. Сервер под блокировкой заявки повторно
проверяет версию, статус и актуальное назначение, затем увеличивает версию и
записывает переход и событие аудита.

### `POST /api/v1/requests/{id}/reject`

Отказывает в проведении испытаний из статуса `registered` или `in_progress`:

```json
{
  "lockVersion": 1
}
```

Действие доступно активному руководителю ИЦ или лаборатории (`WF-006`).
Инициатор уведомляется по email. `403` означает недостаточную роль, `409` —
конкурентное изменение или неподходящий статус, `422` — ошибку тела запроса.

### `POST /api/v1/requests/{id}/withdraw`

Отзывает заявку из статуса `registered`, `in_progress`, `suspended` или
`opinion_preparation` (до контроля СБ):

```json
{
  "lockVersion": 1
}
```

Действие доступно только активному инициатору заявки (`WF-007`). Назначенный
исполнитель и активные руководители ИЦ/лаборатории уведомляются по email.
`403` означает, что актор не является инициатором, `409` — конкурентное
изменение или неподходящий статус, `422` — ошибку тела запроса.

### `POST /api/v1/requests/{id}/color`

Устанавливает ручную цветовую метку строки заявки (не является статусом,
WF-009):

```json
{
  "color": "red",
  "lockVersion": 1
}
```

Допустимые значения: `white`, `red`, `orange`, `blue`, `violet`, `green`.
Действие доступно только активному руководителю ИЦ или лаборатории и работает
в любом статусе заявки. Событие аудита записывается с правилом `WF-009`;
`403` означает недостаточную роль, `409` — конкурентное изменение, `422` —
недопустимый цвет.

### `POST /api/v1/requests/{id}/opinion`

Публикует текст заключения назначенного эксперта:

```json
{
  "body": "Образец соответствует заявленным требованиям.",
  "lockVersion": 5
}
```

Текст должен содержать от 10 до 20 000 символов. Сервер под блокировкой заявки
повторно проверяет активность и текущее назначение эксперта, формирует PDF с
реквизитами заявки и подписью эксперта, сохраняет неизменяемую версию документа,
переводит заявку в `security_review` и увеличивает `lockVersion`. Документ,
переход и аудит фиксируются одной транзакцией; при откате физический PDF удаляется.
`403` соответствует `AUTH-003`, `DOC-005` или `DOC-007`, `409` означает
устаревшую версию, `422` — ошибку тела запроса.

Успешный ответ содержит `status: "in_progress"` и новую `lockVersion`. Ответ
`403` означает нарушение WF-004 или AUTH-003, `409` — запрещённый переход WF-003 или
конкурентное изменение, `404` — отсутствие заявки, `422` — ошибку тела запроса.

## Администрирование

Доступ ко всем эндпоинтам ниже — только активному пользователю с ролью
`Администратор` (AUTH-007); всем остальным `403` с `ruleId: "AUTH-003"`
(неактивен) или `"AUTH-007"` (нет роли).

### `GET /api/v1/admin/users`

Возвращает список всех локальных профилей с ролями:

```json
{
  "items": [
    {
      "id": 1,
      "adLogin": "ivanov",
      "displayName": "Иван Иванов",
      "email": "ivanov@shlz.ru",
      "department": "Испытательный центр",
      "position": "Руководитель ИЦ",
      "isActive": true,
      "roles": [{ "id": 4, "code": "ic_manager", "name": "Руководитель ИЦ" }]
    }
  ]
}
```

### `GET /api/v1/admin/roles`

Возвращает справочник ролей (`id`, `code`, `name`) для форм назначения.

### `POST /api/v1/admin/users`

Заводит локальный профиль заранее, до первого входа сотрудника через LDAP —
можно назначить роль (например, будущему руководителю ИЦ), не дожидаясь,
пока он сам залогинится:

```json
{ "adLogin": "kashin" }
```

`adLogin` — единственное обязательное поле: только латиница, цифры, точка,
дефис, подчёркивание (без `@` и пробелов — bind сам добавит домен).
`displayName` необязателен и на практике не передаётся фронтендом — реальное
имя всё равно придёт из AD при первом входе; если не передан, используется
`adLogin`. Профиль сразу получает базовую роль `Сотрудник` (AUTH-002) — тот
же результат, что и у обычного первого входа; фронтенд после создания сразу
отдельным запросом `POST /api/v1/admin/users/{userId}/roles` назначает роль,
выбранную в форме. При последующем реальном входе через LDAP find-or-create
находит эту же строку по `ad_login` и не трогает назначенные роли (AUTH-004).
Ответ `201` возвращает созданный профиль; `409` — `adLogin` уже занят; `422` —
ошибка валидации.

### `POST /api/v1/admin/users/{userId}/roles`

Назначает роль:

```json
{ "roleId": 4 }
```

Идемпотентно — повторное назначение уже имеющейся роли не создаёт
дублирующую запись и не пишет повторный аудит. Ответ содержит актуальный
список ролей пользователя; `404` — пользователь или роль не найдены.

### `POST /api/v1/admin/users/{userId}/roles/{roleId}/revoke`

Отзывает роль. Идемпотентно — отзыв не назначенной роли не ошибка. Ответ
содержит актуальный список ролей пользователя; `404` — пользователь не найден.

## Диагностика

- `GET /health/live` проверяет только работающий процесс;
- `GET /health/ready` дополнительно проверяет соединение с MariaDB и доступность
  persistent-хранилища документов фактической write/delete-пробой с уникальным
  временным файлом;
- `GET /health/logging` доступен только администраторам в dev-контуре и записывает
  контрольную ошибку Yii в stderr для smoke-проверки контейнерного сбора логов.
